#!/usr/bin/env bash
# dot -- short alias for raw git against the bare dotfiles repo.
# Equivalent to `dotfiles git ...`.
exec /usr/bin/git -C "$HOME" --git-dir="$HOME/.dotfiles" --work-tree="$HOME" "$@"
